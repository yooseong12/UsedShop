version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/UsedShop
    overwrite: yes

permissions:
  - object: /
    pattern: "**"경
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 60
      runas: ubuntu